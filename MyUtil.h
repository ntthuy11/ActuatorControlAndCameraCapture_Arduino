
String utilConvertByteToString(byte n) {
  char str[255];
  sprintf(str, "%d", n);
  return str;
}

String utilConvertIntToString(int n) {
  char str[255];
  sprintf(str, "%d", n);
  return str;
}

